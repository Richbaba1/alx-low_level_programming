#include <stdio.h>
/**
 * main - this program prints single digit numbers in base 10
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	int r;

	for (r = 0; r < 10; r++)
	{
		printf("%d", r);
	}
	printf("\n");
	return (0);
}
